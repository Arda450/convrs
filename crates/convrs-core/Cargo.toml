[package]
name = "convrs-core"
version = "0.1.0"
edition = "2024"
description = "Core-Bibliothek für Datenformat-Konvertierung (JSON, YAML, TOML, CSV)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.8"
csv = "1.3"
